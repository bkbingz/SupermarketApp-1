<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Movie - Phoenix</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #ff5722;
      color: #111;
      font-family: 'Segoe UI', sans-serif;
    }

    .navbar {
      background-color: #000;
    }

    .navbar-brand,
    .navbar-nav .nav-link {
      color: #ff5722 !important;
      font-weight: bold;
    }

    .navbar-nav .nav-link:hover {
      text-decoration: underline;
    }

    h2 {
      color: #000;
      margin-top: 20px;
      font-weight: bold;
    }

    label {
      color: #000;
      font-weight: 600;
    }

    .form-control {
      background-color: #000 !important;
      color: #fff !important;
      border: 1px solid #444;
    }

    .form-control:focus {
      border-color: #fff;
      box-shadow: 0 0 0 0.2rem rgba(255, 255, 255, 0.25);
    }

    .btn-primary {
      background-color: #000;
      border: none;
      color: #ff5722;
      font-weight: bold;
    }

    .btn-primary:hover {
      background-color: #222;
      color: #fff;
    }

    img.preview {
      width: 120px;
      margin-bottom: 15px;
      border-radius: 6px;
      background-color: #000;
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="/images/phoenix.png" alt="Phoenix Logo" width="32" height="32" />
        Phoenix
      </a>
    </div>
  </nav>

  <div class="container mt-4">
    <h2>Update Movie</h2>

    <form action="/updatemovie/<%= movies.movieId %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="moviename">Movie Name:</label>
        <input type="text" id="moviename" name="moviename" class="form-control" required value="<%= movies.moviename %>">
      </div>

      <div class="mb-3">
        <label for="synopsis">Synopsis:</label>
        <textarea id="synopsis" name="synopsis" class="form-control" rows="3" required><%= movies.synopsis %></textarea>
      </div>

      <div class="mb-3">
        <label for="seatcapacity">Seat Capacity:</label>
        <input type="number" id="seatcapacity" name="seatcapacity" class="form-control" required value="<%= movies.seatcapacity %>">
      </div>

      <div class="mb-3">
        <label for="showday">Show Day:</label>
        <input type="date" id="showday" name="showday" class="form-control" required value="<%= movies.showday.toISOString().split('T')[0] %>">
      </div>

      <div class="mb-3">
        <label for="showtime">Showtime:</label>
        <select id="showtime" name="showtime" class="form-control" required>
          <option value="09:30" <% if (movies.showtime === '09:30') { %> selected <% } %>>09:30 AM</option>
          <option value="12:00" <% if (movies.showtime === '12:00') { %> selected <% } %>>12:00 PM</option>
          <option value="15:30" <% if (movies.showtime === '15:30') { %> selected <% } %>>03:30 PM</option>
          <option value="19:30" <% if (movies.showtime === '19:30') { %> selected <% } %>>07:30 PM</option>
          <option value="22:00" <% if (movies.showtime === '22:00') { %> selected <% } %>>10:00 PM</option>
        </select>
      </div>

      <div class="mb-3">
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" class="form-control" step="0.01" required value="<%= movies.price %>">
      </div>

      <div class="mb-3">
        <label for="genre">Genre:</label>
        <input type="text" id="genre" name="genre" class="form-control" required value="<%= movies.genre %>">
      </div>

      <div class="mb-3">
        <label for="rating">Rating:</label>
        <input type="text" id="rating" name="rating" class="form-control" required value="<%= movies.rating %>">
      </div>

      <div class="mb-3">
        <label for="runtime">Runtime:</label>
        <input type="text" id="runtime" name="runtime" class="form-control" required value="<%= movies.runtime %>">
      </div>

      <div class="mb-3">
        <label for="opening">Opening Date:</label>
        <input type="date" id="opening" name="opening" class="form-control" required value="<%= movies.opening.toISOString().split('T')[0] %>">
      </div>

      <div class="mb-3">
        <label for="image">Movie Image:</label><br>
        <% if (movies.image) { %>
          <img src="/images/<%= movies.image %>" class="preview" alt="Current Image"><br>
        <% } %>
        <input type="hidden" name="currentImage" value="<%= movies.image %>">
        <input type="file" name="image" class="form-control mt-2">
      </div>

      <button type="submit" class="btn btn-primary">Update Movie</button>
    </form>
  </div>

</body>
</html>
